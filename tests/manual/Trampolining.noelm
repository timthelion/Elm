import open Trampoline
import open Either

fac' : Int -> Int -> Trampoline Int
fac' n acc = case n of
  0 -> Trampoline . Left <| acc
  _ -> Trampoline . Right <| \() -> fac' (n - 1) (acc * n)

fac : Int -> Int
fac n = trampoline (fac n 1)

main = asText . fac <| 10000000